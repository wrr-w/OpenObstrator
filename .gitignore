__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.log
.claude/settings.local.json
.venv/

# config / runtime state
data/config.yaml
data/registry.json

# server host/port config (per-machine)
openobstrator.server.json

# instance specific data
data/nanoghost/instances/*/

# environment secrets
*.env

# frontend dependencies
node_modules/

# build artifacts
build/
dist/
dist.7z
dist_data_bak/
scripts/_version_info.txt
# 运行期数据不进仓库（config.yaml / registry.json / downloads / 纳管实例 / .env）。
# 保留 .gitkeep 让目录存在，源码构建（build.spec 打包 data/）才不会挂。
data/
!data/.gitkeep
*.bak
server/static/g6.min.js

# runtime files
server.pid
*.log
server_start.log
server_start_err.log
server_stdout.log
server_stderr.log
ngrok_openobstrator.log
